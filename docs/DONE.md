# DONE

Completed development steps, most recent last.

## Decisions

- Package name: `@ya8d/dsh-directive-compact` (replaces the earlier `@ya8d/dsh-compaction-directive`; the old repository is retired).
- Pure increment: the plugin never touches `ctx.compaction` — no `BasicCompactionEngine` inheritance, no single-slot registration, no automatic-compaction listeners. The upstream backend and `/compact` stay untouched.
- Commands: `/compact-directive <requirement>` (keep head + recent turns verbatim, summarize the middle with a directive layered over a four-point baseline) and `/trim-directive <requirement>` (hand the whole conversation to the model with a directive-only prompt, zero dialogue-region protection) — both global commands registered in a plain context so every preset and headless see them.
- Single-path compaction (ContextForge semantics): the tail floor (last user utterance preserved verbatim) makes any degraded path unreachable, so the design is one path — fixed-skeleton head + recent turns verbatim, middle summarized.
- Fixed-skeleton head: the first `user` message plus the injected `agent-instructions` / `system-prompt` / `skill-catalog` nodes — established by a real-session survey (14+ sessions, including a fresh one-turn session). These nodes appear once at session start, are not re-injected per turn, and are not restored after a replacement, so they are preserved.
- Middle-span replacement through `session.append` with `surfaceOp: { op: 'replace' }`; session-side append validation (provenance, source-event coverage, tool-result rewrite rules) enforces integrity.
- Lifecycle events `compaction/start` / `compaction/summary` / `compaction/end` are appended; the checkpoint message carries `{ kind: 'plugin', plugin: 'compact', compactionId, sourceCommandId }`.
- Trim budget: fixed per-chunk input 50K heuristic tokens and max 20 chunks (20 × 50K = 1M, the target window) — the plugin targets the 1M-window DeepSeek models; output cap stays `min(window/2, 256K)` with per-chunk retry (3 attempts). (P10.1 superseded the window-derived `window/5 = 200K` / 10 chunks of P8.)
- Workflow: the agent creates branches and commits; the maintainer (ya8D) pushes and opens PRs.

## Steps

- [x] P0: scaffolded the new repository at `C:\AI\dsh-directive-compact` (independent of the retired `C:\AI\dsh-compaction-directive`).
- [x] P0: `package.json` (`@ya8d/dsh-directive-compact`, ESM, `dsh.bundle`, minimal peers, `files` whitelist), tsconfigs (strict NodeNext), vitest configs, `LICENSE`, `.gitignore`.
- [x] P0: `src/invariant.ts` (no-runtime-invariant companion with reason), `src/index.ts` (function-plugin skeleton, no default export).
- [x] P0: repo standards — `AGENTS.md`, `docs/AGENTS.md`, `.agents/notes/README.md`; committed as `5d8b956`.
- [x] P0: Agent Note `implemented/architecture/2026-08-15-two-path-directive-compaction.md` — records the keep-head-and-recent-turns design and its alternatives (a duplicate note I drafted was deleted in favor of this one).
- [x] P0: README — contract, install (`dsh plugin add`), command, Model Experience, Known Limitations, implementation status; committed on `feat/readme` as `832a847` (awaiting maintainer push/PR).
- [x] P0: `docs/TODO.md` / `docs/DONE.md` workflow log.
- [x] P1: `src/plan.ts` — pure range-planning functions: `headBoundaryIndex` (later renamed `skeletonEndIndex`; fixed-skeleton head = first user utterance + following injected system nodes), `splitTurns` (user-led turns; deleted in P5 as dead code), `planCompaction`. Deliberately session-free (input is a flat node description) so every decision is unit-testable; balance checking is deferred to the command layer (P3) where the `Session` is available.
- [x] P1: `tests/plan.spec.ts` — 21 tests: head boundary (fresh / compacted / empty / injected-follow), turn splitting, middle selection, tail floor, `keepRecentTurns=0` edge. Fixed a `-0` slice trap in `planCompaction` (`slice(0, -0)` returns empty) and a wrong test expectation.
- [x] P1: design refinement — the tail always covers the last user utterance and everything after it (in-flight assistant stream / unpaired tool call survive), so `keepRecentTurns` only extends the tail further back. This makes the planned degraded path unreachable, so the design is single-path; the Agent Note was updated to match.
- [x] P1: review corrections — dropped the unused `hasDirective` parameter (no current owner; YAGNI), replaced the hard-coded system-node kind enumeration with a `kind !== 'user'` injection test (robust to upstream node-name changes and to a preceding `compact` checkpoint), and documented the tail-floor invariant on `planCompaction` (tail non-empty whenever a user utterance exists; a no-user conversation may have an empty tail).
- [x] P1: design change — turn-marker localization was falsified by real data. A twice-compacted session keeps 101 `turn/start`/`turn/end` markers in the log while its surface anchors on 57 live user utterances; "the Nth turn" is meaningless after compaction. `planCompaction` now anchors on surface user utterances with configurable `keepHeadUsers` (default 3) and `keepTailUsers` (default 3) via `PlanConfig`; a prior `compact` checkpoint is absorbed into the skeleton head. Verified on the real session: head 46 nodes, middle 974, tail 23, last user in tail.
- [x] P1: typecheck + build green; committed on `feat/P1-planning` (awaiting maintainer push/PR).
- [x] P2: `src/summarizer.ts` — clean single-message directive summarization. `FOUR_POINT_INSTRUCTION` (ContextForge `_SUMMARY_PROMPT`: task goal / key steps / findings / next step + verbatim `[user]` utterances; the `[user]` marker is a named contract shared with `renderSpan` via `ROLE_PREFIX`), `buildSummaryPrompt` (directive layered over the baseline, never replacing it), `renderSpan` (role-prefixed plain text with tool-call/tool-result/image handling), `summarizeWithDirective` (BlockAssembler clean call, image rejection, no-text rejection, finish-error mapping for error/aborted/max-tokens, oversized-input guard), `checkpointMarker` / `CHECKPOINT_GUARD`. The ContextForge `_build_directive_only_prompt` port (`directiveInstruction`) was dropped per review — P1 established the single-path design, so it had no caller.
- [x] P2: `tests/summarizer.spec.ts` — 13 tests: prompt construction (with/without directive), renderSpan (text + tool blocks), marker/guard, image rejection, no-text rejection, max-tokens and error finish mapping, oversized-input guard.
- [x] P2: typecheck + build green (32 tests total); changes staged on `feat/P2-summarizer` (awaiting maintainer review; not committed per workflow).
- [x] P3: `src/command.ts` — the transaction. `surfaceNodes` (session surface → node descriptors with source kinds), `resolveDirectiveTarget` (configured pair → routed header → agent options fallback), `executeDirectiveCompact` (planCompaction → summarize middle via `summarizeWithDirective` → `session.append` lifecycle `compaction/start` / `compaction/summary` / `user/message` replace / `compaction/end`; the checkpoint message carries `compactCheckpointSource`; a failed attempt still closes the lifecycle with the error).
- [x] P3: `src/index.ts` — the function-plugin entry: `Config` schema (`keepHeadUsers`/`keepTailUsers` default 3, `summarizationProvider`/`summarizationModel` default empty, `maxTokens` default 8192), `inject = ['commands','sessions','llm','tokenMeter']`, `apply` registers `/compact-directive` via `ctx.effect` with handler drain. Added `@deepseek-ai/schemastery@3.18.1` as a runtime dependency.
- [x] P3: `tests/command.spec.ts` — 6 tests over a real `Session` (head/middle/tail plan honored, lifecycle event sequence, checkpoint source, error closes lifecycle); `tests/loader-composition.spec.ts` — 3 tests (Loader boots the composition and registers the command, no-default-export assertion, HMR-safety dispose → unregister).
- [x] P3: typecheck + build green (41 tests total); changes staged on `feat/P3-command` (awaiting maintainer review; not committed per workflow).
- [x] P3 review fixes: (1) capture `compaction/start` at append time and carry its seq through the transaction instead of re-reading `session.events[length-1]` after the async summarize window — removes a race with concurrent appends; (2) refuse standalone compaction while a turn is open (`openTurnNumber` scan → `DirectiveCompactionError('busy')`) so the `turn: null` invariant cannot trip; (3) `DirectiveCompactionError` with codes `busy`/`cancelled`/`summary`/`commit` (commit-vs-summary distinguished by a stage flag), translated to human outcomes in the command handler — reusable by P4 automatic compaction; (4) tests for open-turn busy refusal and an empty-content middle node.
- [x] P4: regression tests — (1) a cancelled summarization (stream aborts mid-flight) leaves the surface intact and the lifecycle closed with `compaction/end`; (2) a failed commit (finish error) never leaves a partial replace — surface unchanged, events after `compaction/start` are exactly `['compaction/start','compaction/end']`.
- [x] P4: `cordis.patch.yml` — insert-only bundle row (`- insert: - id: directive-compact, name: '@ya8d/dsh-directive-compact'`); disables nothing, pure increment. `--dump-config` verification is queued in P5.
- [x] P4: full suite green — 45 tests (plan 19 + summarizer 13 + command 10 + loader-composition 3); `tsc -p tsconfig.json` and `tsc -p tsconfig.test.json` both clean. Changes staged on `feat/P4-polish` (awaiting maintainer review; not committed per workflow).
- [x] P5: cleaned the user's headless profile — removed the retired `@ya8d/dsh-compaction-directive` bundle (it pointed at the deleted old repo and broke profile resolution), installed the new plugin with `dsh plugin --profile headless add C:\AI\dsh-directive-compact`, and verified `dsh --profile headless --dump-config` composes the `# == @ya8d/dsh-directive-compact` layer with the `directive-compact` row — insert-only, no upstream row disabled.
- [x] P5: with-key e2e (`tests/directive-compact.e2e.ts`) — real `LlmRuntime` + `LlmDeepSeek` + `TokenMeter` composition, key resolved from `$DEEPSEEK_API_KEY` or the `$DSH_HOME/.credentials.yaml` document (matching the launched product); runs one real `executeDirectiveCompact` against `deepseek-v4-flash` and asserts the lifecycle sequence, real provider/model/usage facts, surface shrink, and compact checkpoint source. Self-skips without a key (verified both arms: real call passes ~4.5s, keyless run reports 1 skipped). `tsc -p tsconfig.test.json` clean with the e2e file included.
- [x] P5: README truth pass — dropped the stale two-path description (P1 established single-path: head + tail floor + summarized middle), fixed the false claim that a `compaction/directive-before-after` event is recorded (no such event exists; only the standard lifecycle is logged), and updated implementation status to shipped P1–P4 + verified P5 bundle/e2e.
- [x] P5: dead-code cleanup — removed `splitTurns` from `src/plan.ts` (no production caller; only its own unit tests exercised it, and its JSDoc claimed a P3 consumer that never materialized) and the corresponding 3 tests; corrected the `planCompaction` JSDoc claim that the command layer re-checks balance with a session pairing API (it does not — boundaries fall at user utterances by construction). Unit suite now 42 tests (plan 16 + summarizer 13 + command 10 + loader-composition 3), plus the 1 e2e test.
- [x] P5: README `## Testing` section — states the two-command split explicitly: `npm test` collects only `tests/**/*.spec.ts` (unit, no network), while `npm run test:e2e` collects the e2e file and self-skips without a key. This addresses the reviewer observation that "self-skips without a key" was accurate only once the e2e file is collected — the default command never loads it.
- [x] P6: `/trim-directive` — AI natural-language trim (final shape; an earlier keyword/regex iteration was rejected by the user's review and reworked). Design confirmed with the user: English directive-only prompt (ContextForge `compact_by_directive` shape, no four-point baseline), shrink validation retained, injected system nodes are NOT trim-able content.
- [x] P6: `src/trim.ts` — `TRIM_INSTRUCTION` + `buildTrimPrompt(directive)` (requirement verbatim, no baseline; empty requirement fails loud) + `trimMarker`. Removed the keyword `compileTrimPattern`/`planTrim` entirely.
- [x] P6: `src/summarizer.ts` — `summarizeWithDirective` gained optional `promptBuilder` (default `buildSummaryPrompt`) and `markerBuilder` (default `checkpointMarker`) parameters; all safety logic (BlockAssembler, image/no-text rejection, finish mapping, oversized guard) reused by both commands.
- [x] P6: `src/command-trim.ts` — `executeTrim` rewrite: empty input errors; busy refusal; trim range = after the LAST injected system node through the surface tail (`isInjectedSystemNode` classifies plugin-sourced `user/message`; the opening anchor stays outside as a contiguous-replace structural consequence, not a protection policy); boundaries expanded to `toolPairingBalancedBefore`/`After`; full lifecycle `compaction/start` (turn null) → render → LLM call → shrink check (checkpoint must be smaller than shadowed) → `compaction/summary` → `user/message` replace (`compactCheckpointSource`, sourceEventSeqs covers all shadowed + start/summary) → `compaction/end`; failure closes the lifecycle. Three system-node guards: render exclusion, range exclusion, session append validation fail-loud.
- [x] P6: `src/index.ts` — trim command description updated to "trim the whole conversation per a natural-language requirement".
- [x] P6: `tests/trim.spec.ts` — 13 tests: buildTrimPrompt (verbatim requirement, no baseline, empty fails), isInjectedSystemNode classification, whole-range replacement + lifecycle sequence + checkpoint source, system nodes preserved (render + range), directive verbatim in LLM prompt, shrink rejection, empty input, busy refusal, summarization failure closes lifecycle, cancelled trim leaves surface intact.
- [x] P6: loader-composition tests assert both commands register and both unregister on dispose; package.json `files`/description cover the new modules.
- [x] P6: full suite green — 55 tests (plan 16 + summarizer 13 + command 10 + trim 13 + loader-composition 3); `tsc -p tsconfig.json` and `tsc -p tsconfig.test.json` clean. Changes staged on `feat/P6-trim` (awaiting maintainer review; not committed per workflow).
- [x] P7: `/compact-directive` requires the directive — an empty `rawInput` returns a usage error pointing at `/compact` (mirror of upstream's "no arguments" contract) instead of silently degrading to the weaker plain four-point summary; the refusal happens before `compaction/start`, so no lifecycle opens.
- [x] P7: shrink validation — the framed checkpoint must be smaller than the shadowed span (`DirectiveCompactionError('summary')` otherwise), mirroring the upstream convergence check; the checkpoint is built once and reused for the replace.
- [x] P7: unit tests (command.spec.ts +2) — empty-directive refusal (no lifecycle opened) and deterministic shrink rejection (lifecycle closed, no replace landed, user/message count unchanged).
- [x] P7: e2e updated — the real-model run legitimately hit the shrink rejection (deepseek-v4-flash emitted an 1804-token summary over a 1428-token fixture span), which proved the validation works end to end; the e2e now asserts both correct outcomes (success + full lifecycle + checkpoint, or shrink rejection + lifecycle closed + surface unchanged). Empty-directive e2e added (no model call, no lifecycle, no surface change).
- [x] P7: full suite green — 57 unit tests (plan 16 + summarizer 13 + command 12 + trim 13 + loader-composition 3) + 2 e2e; `tsc` both configs clean. Changes staged on `feat/P7-polish` (awaiting maintainer review; not committed per workflow).
- [x] P7 review fixes: (1) shrink tests no longer depend on `estimateMessage` call order — both command.spec.ts and trim.spec.ts now key the fake meter on the message CONTENT (checkpoint carries the directive marker/guard; shadowed conversation messages do not), so a future change to how many times the meter is called cannot silently desync the threshold; (2) both commands' empty-directive rejections now carry a comment stating why they return a structured `kind: 'error'` (parameter validation, before any lifecycle opens) while lifecycle-time failures (shrink, summarization) throw — the two failure styles are distinct on purpose; (3) the e2e's dual-outcome assertion gained a comment noting the shrink-rejected branch is model-dependent and that deterministic shrink coverage lives in the unit suite.
- [x] P8: budgeted chunked trim (user-confirmed v2 design). Feasibility verified against dsh source: `tokenMeter.measure().nodes[].tokens` (chunk unit), `llm.resolveModelInfo().context.contextWindow` (window, `Maximum combined request and response context`), adapter `DEFAULT_MAX_TOKENS = 256_000`, `llm.stream()` parallel-safe (independent generators, no session lock), `createUserMessage` accepts multi-block content, 429 handled by retryPolicy.
- [x] P8: `src/trim.ts` — `resolveTrimBudget` (maxTokens = min(W/2, adapterMax) = 256K, chunkInputBudget = W/5 = 200K, maxChunks = 10 over the decimal 1,000,000-token window; worst case = a ~101K node per chunk → ceil(1M/101K) = 10) and `chunkTrimNodes` (per-node token accumulation, cut roll-back to `toolPairingBalancedBefore`, oversized single node taken whole, total-input > chunks×budget fails loud with "compact the session first").
- [x] P8: `src/command-trim.ts` — `executeTrim` now resolves the routed window, prices the shadowed span once via `measure`, slices into chunks, summarizes all chunks in parallel (`Promise.all`), and assembles one checkpoint: marker + guard once, each chunk's text under `[part N/M]` with per-chunk marker/guard repeats stripped, usage merged across chunks, shrink check on the assembled checkpoint.
- [x] P8: `tests/trim.spec.ts` — 23 tests (10 new): budget math (window/2 vs adapter cap, invalid inputs), chunking (single/multi chunk, balanced roll-back, oversized node, fail-loud over bound, empty), and a parallel-dispatch test asserting 2 stream calls for a 2-chunk surface, `[part 1/2]`/`[part 2/2]` in the assembled checkpoint, single head marker, merged usage.
- [x] P8: with-key e2e — `/trim-directive` real DeepSeek run asserting lifecycle + skeleton survival on success, or shrink rejection + lifecycle closed + surface unchanged; suite now 3 e2e tests.
- [x] P8: Agent Note `implemented/architecture/2026-08-15-chunked-trim-budget.md`; README chunking section + Known Limitations (heuristic pricing, parallel token cost identical to serial); package.json `files` unchanged (trim.ts already shipped).
- [x] P8: full suite green — 67 unit tests (plan 16 + summarizer 13 + command 12 + trim 23 + loader-composition 3) + 3 e2e; `tsc` both configs clean. Changes staged on `feat/P8-chunked-trim` (awaiting maintainer review; not committed per workflow).
- [x] P8 real-session verification (session "插件测试", `session-71c666f1`): the user ran `/trim-directive` twice on the same 36-node surface. First run (pre-P8 code) FAILED after 76s with `directive summarization truncated at the token cap` — the exact bug the budget fixes. Second run (P8 code) SUCCEEDED in ~4.5 min: replaced 28 nodes (shadowedTokenCount 9,876) with one checkpoint that kept the two user directives verbatim and dropped the git/doc-edit process records; the injected skeleton and pre-injection nodes survived. Usage: inputTokens 88, cacheReadTokens 12,928 (single-chunk replay hit), outputTokens 36,874 of which reasoningTokens 34,244 — **92.9% of the output was thinking**. Because thinking is NOT disabled for trim (by design; `reasoningEffort: 'off'` maps to `thinking: 'disabled'` and would hurt cut decisions), a custom trim on a complex natural-language directive takes tens of seconds to minutes: the model reasons heavily before writing the trimmed context. This confirms the budget's key assumption (256K output must leave room for thinking) and explains why the old 8,192 maxTokens truncated. Chunking itself did not trigger here (9,876 tokens << 200K chunk budget) — this run exercised the single-chunk path plus the budget fix.
- [x] P8 review fixes: (1) TODO.md design numbers synced to the shipped budget — the P8 design section now states `W/5 = 200K` per-chunk input and `maxChunks = 10` (it had drifted to the v2 draft's `W/4 = 250K` + 8); (2) the silent `contextWindow ?? 1_000_000` fallback became fail-loud — a model reporting no context window now throws a `summary`-class error before `compaction/start` opens (a silent 1M assumption would over-budget a smaller-window model), with a unit test asserting no lifecycle opens; (3) confirmed non-issues: `mergeUsage` summing reasoningTokens is the intended total-thinking semantic, rawOutput aggregation is audit-only and matches `llmStreamCall: true`, and the e2e `surfaceText` helper ignoring non-text blocks is sufficient for skeleton-survival assertions. Suite now 68 unit tests (trim 24).
- [x] P8 parallel-chunk verification (session "插件并发测试1", `session-40bc11be`): user ran `/trim-directive` on a >200K surface. First run FAILED with `produced no text summary content` — user-confirmed cause: a network/proxy switch mid-run, NOT a chunking bug (the failure closed the lifecycle, surface unchanged — correct atomicity). Second run SUCCEEDED and is the first real exercise of parallel chunking: shadowed **1,055 nodes / 402,837 tokens** (>200K budget) → **3 chunks in parallel**, assembled into one checkpoint with `[part 1/3]`/`[part 2/3]`/`[part 3/3]`, marker + guard once, sourceEventSeqs 1,057 (all shadowed + start + summary). Usage: inputTokens 252, cacheReadTokens 509,696 (3 parallel calls each replaying a prefix), outputTokens 23,279 of which reasoningTokens 17,032 (~73%), body ~6,247 << 402,837 → shrink passed. The model kept the user's original directives and the plugin-development context per the requirement.
- [x] P9 retry: added `summarizeChunkWithRetry` — each chunk gets up to 3 attempts (1 initial + 2 retries) for transient failures (network hiccup, proxy switch, adapter 5xx) so one flaky chunk cannot sink a whole parallel trim; cancellation/abort and `DirectiveCompactionError` are never retried. Tests: transient failure retries and succeeds (2 stream calls), persistent failure gives up after 3 attempts with the lifecycle closed. Suite now 71 unit tests (plan 16 + summarizer 13 + command 12 + trim 27 + loader-composition 3).
- [x] P9 review fixes: (1) added the abort-short-circuit test — aborting during the first attempt stops the retry loop immediately (streamCalls === 1, no second call, lifecycle closed), locking the `signal.aborted` branch in the retry loop; (2) recorded the retry policy on `MAX_TOKENS` truncation: it IS retried (it is a plain `LlmError`, not `DirectiveCompactionError`) — deliberate, because the window-derived 256K budget makes deterministic truncation rare while transient mid-stream truncation is worth one retry; documented in the helper's JSDoc; (3) fixed a formatting regression in `mergeUsage` (parameter list had collapsed onto the signature line); (4) confirmed the README blockquotes (log-only commands, original dialogue stays visible) match the source-verified `surface.ts` behavior and upstream `/compact` parity. Suite now 71 unit tests (plan 16 + summarizer 13 + command 12 + trim 27 + loader-composition 3).
- [x] P10: full-freedom trim range (user-confirmed). Motivation: "精确删除测试2" showed the trim start ("after the last injected node") misfiring on forked/compacted sessions — a real session is `compact checkpoint[0] → dialogue → skeleton[5-7] → dialogue…` because automatic compaction starts at `surfaceNodes[0]` (region.ts) and the skeleton re-injects mid-conversation. Source-verified that the skeleton regenerates per request: agent-loop `preStep` calls `systemPrompt.assemble()` and projects a context message; `agent-instructions` composes into `agent/pre-step` request messages — both per-request, not surface-persistent. So `selectTrimRange` now returns the WHOLE surface; deleted `isInjectedSystemNode`. Only tool-pairing balance remains (session-integrity). Tests updated (skeleton no longer preserved, whole-surface render, parallel-chunk counts); e2e updated. Agent Note + README rewritten for full freedom. Suite now 69 unit tests (trim 25; removed 2 isInjectedSystemNode tests).
- [x] P10.1: fixed 50K chunk budget (user-confirmed). Motivation: "复杂对话 (1)" (`session-43a12820`) — `/trim-directive 删除telemetry相关的内容` on a 199-node / ~178K-token surface (below the 200K budget) opened `compaction/start` and produced no summary/end for 10+ minutes (hung on the single ~178K-token call). User proposed: per-chunk limit 50K, split as soon as the accumulation first exceeds it, max 20 chunks, full parallel. Verified against dsh source: no llm-layer concurrency limiter (20 parallel `llm.stream()` calls are safe; 429s handled by adapter retryPolicy + per-chunk retry); per-call input+output ≤ 1M window even with CJK underestimation (meter prices 4 chars/token); render ~200K chars/chunk far under the 4M-char guard. `resolveTrimBudget` now returns `chunkInputBudget = 50_000` / `maxChunks = 20` (20 × 50K = 1M; total beyond it fails loud); `chunkTrimNodes` cut semantics unchanged (first-fit accumulation to the budget + balanced roll-back). Tests resized to the 50K budget (20K nodes for multi-chunk, 15K for the balanced roll-back, 60K oversized node); README + Agent Note `2026-08-15-chunked-trim-budget.md` revised; TODO P11 latency item marked partially mitigated.
- [x] P10.2: phase logging for both commands (user request: diagnose "哪里卡住了，哪里耗时", following upstream conventions). Verified conventions in harness source: Cordis `ctx.logger(name)` named facade; `info/warn/debug` with `%s/%d` placeholders; console default threshold INFO (LoggerLevel ERROR=0/INFO=1/WARN=2/DEBUG=3); per-scope override via logger-console `levels: { 'dsh-directive-compact': 3 }`; upstream `compaction-basic` logs results at info and failures at warn. Added `src/log.ts` (`shortDirective`, 120-char cap). `command.ts`: `begin` / `summarization done in Nms` / `committed` (info) + failure `warn` + refusal `debug`. `command-trim.ts`: `begin` (directive/surface/budget) / `priced … → N chunk(s)` / per-chunk `done in Nms (output tokens)` / `all N chunks done in Nms` / `committed` (info) + failure and retry `warn` + chunk geometry `debug`. `summarizer.ts`: `debug` time-to-first-chunk + stream duration (locates network/provider latency vs generation). Tests: `tests/helpers.ts` `createLoggerStub` (callable + method-bearing, records raw printf args) wired into every fake ctx; +3 log-assertion tests. README `Observability` section (line semantics + how to enable debug); CHANGELOG entry. Rendered every log line through Cordis `Logger.format` to confirm placeholder alignment (incl. CJK directives). Suite now 72 unit tests + 3 e2e, both tsc configs and build green.
- [x] P10.3: no-change is a success, not a failure (user hypothesis on the "复杂对话 (1)" hang, confirmed against code). Mechanism: "删除telemetry相关的内容" found no telemetry → the model returned the context essentially verbatim → under the old single-200K-chunk path the near-verbatim rewrite could hit `MAX_TOKENS` (thinking + ~178K output > 256K cap), a plain `LlmError` that the per-chunk retry loop retries 3 times — each attempt minutes long, 10-20 min total with no session events; even without truncation, shrink validation threw a `summary` error. User decision: an unshrunk output is OK; report it, don't retry or fail. Verified `compaction/invariant.ts` accepts a success `compaction/end` (no error) with a `compaction/summary` but no replace. Both commands now append `compaction/summary` (recording the model output) BEFORE the shrink check; when `checkpoint >= shadowed` they append a clean `compaction/end`, log `no change` at info, and return success (`Nothing to trim: …` / `Nothing to compact: …`) with the surface untouched — no retry loop can start. Unit shrink tests rewritten to the no-change lifecycle; e2e's shrink-rejected branch became a no-change branch, the fixture gained `step/start`/`step/end` (the real token meter requires a matching step for `assistant/message` — the old catch branch had silently absorbed that error), and the trim directive now targets real `git workflow detail` fixture content (`drop all dialogue, keep nothing` could return reasoning-only and fail with `produced no text summary content`). README "When there is nothing to remove" section. Suite 72 unit + 3 e2e (real key), both tsc configs and build green.
- [x] Review round (50K chunk budget, user decision "只考虑1M模型"): (1) documented that the 256K output cap is per chunk with NO cross-chunk aggregate cap, bounded in practice by the shrink validation on the assembled checkpoint (must be < shadowed) — added to `resolveTrimBudget` JSDoc and README Known Limitations; (2) NO small-window guard added — the plugin targets the 1M-window DeepSeek models only, so the fixed 50K chunk cannot exceed the window by construction and the no-context-window fail-loud stays as the last defense; (3) deleted the dead-branch `resolveTrimBudget` test (1M + 600K adapter — the target adapter is always 256K < 500K, so the window branch of the min is unreachable) and documented that in the retained test. Suite 71 unit + 3 e2e, both tsc configs and build green.
